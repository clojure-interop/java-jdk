(ns jdk.security.cert.Certificate$CertificateRep
  "Alternate Certificate class for serialization."
  (:refer-clojure :only [require comment defn ->])
  (:import [java.security.cert Certificate$CertificateRep]))

