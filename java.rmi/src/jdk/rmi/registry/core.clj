(ns jdk.rmi.registry.core
  (:refer-clojure :only [require comment defn ->])
  (:import ))

(require '[jdk.rmi.registry.LocateRegistry])
(require '[jdk.rmi.registry.Registry])
(require '[jdk.rmi.registry.RegistryHandler])
