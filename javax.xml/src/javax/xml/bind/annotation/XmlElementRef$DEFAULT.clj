(ns javax.xml.bind.annotation.XmlElementRef$DEFAULT
  "Used in XmlElementRef.type() to
  signal that the type be inferred from the signature
  of the property."
  (:refer-clojure :only [require comment defn ->])
  (:import [javax.xml.bind.annotation XmlElementRef$DEFAULT]))

(defn ->default
  "Constructor."
  (^XmlElementRef$DEFAULT []
    (new XmlElementRef$DEFAULT )))

